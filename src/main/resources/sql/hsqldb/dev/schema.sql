DROP TABLE USER IF EXISTS;
CREATE TABLE USER(ID BIGINT GENERATED BY DEFAULT AS IDENTITY, NAME  VARCHAR(50),ADDRESS VARCHAR(40),CITY VARCHAR(35),STATE VARCHAR(2), PIN integer, PRIMARY KEY (ID), UNIQUE(PIN));


